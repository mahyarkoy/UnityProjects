using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TreeSharpPlus;
public class StoryController : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
	/*
	public Node  Protected ST_Story_one_controller()
	{
		return new SelectorParallel (ST_Chekcing_current_story(current_story),ST_story_one_arc)
	}
	
	Protected ST_Story_two_controller()
	{
		return new SelectorParallel (ST_Chekcing_current_story(current_story),ST_story_two_arc)
	}
	Protected ST_Story_three_controller()
	{
		return new SelectorParallel (ST_Chekcing_current_story(current_story),ST_story_three_arc)
	}
	Protected ST_Story_four_controller()
	{return new SelectorParallel (ST_Chekcing_current_story(current_story),ST_story_four_arc)
	}*/
}
